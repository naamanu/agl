language "0.3";
pipeline identity(value: String) -> String { return value; }
pipeline main(flag: Bool) -> String { if flag { return "early"; } else { let answer = identity("late"); } return answer; }
